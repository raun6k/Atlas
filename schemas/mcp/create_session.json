{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://atlas.dev/schemas/mcp/create_session.json",
  "title": "create_session",
  "type": "object",
  "additionalProperties": false,
  "required": ["subject_reference", "delivery_serviceability_reference"],
  "properties": {
    "subject_reference": { "type": "string" },
    "delivery_serviceability_reference": {
      "type": "string",
      "minLength": 1,
      "description": "Opaque Host delivery reference that must match an active location. Atlas does not fall back to a default neighbourhood."
    },
    "locale": { "type": "string" },
    "requested_location_id": { "type": "string" },
    "evaluation_arm": {
      "type": "string",
      "enum": ["CONTROL", "TREATMENT"],
      "description": "Host-stamped experiment arm. AtlasLab Host overwrites any Buyer-supplied value before signing (ID-600)."
    },
    "strategy_allowlist": {
      "type": "array",
      "items": { "type": "string", "enum": ["FREE_DELIVERY", "SMALL_ORDER", "BRAND_PROMO", "FBT"] },
      "description": "Host-stamped demo strategy allowlist. Empty on TREATMENT resolves to all enabled DEMO strategies; CONTROL uses an empty allowlist. AtlasLab Host overwrites any Buyer-supplied value. Exploratory strategies are rejected."
    }
  }
}
